import { BorderTaskDirective } from './border-task.directive';

describe('BorderTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderTaskDirective();
    expect(directive).toBeTruthy();
  });
});
